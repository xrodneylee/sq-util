sshs:
  description: Generate SSH public and secret keys.
  input:
    key size (bits):
      type: select
      option:
        - 2048
        - 4096
  elem2:
    - data
gunpg:
  description: gunpg
  input:
    key size (bits):
      type: select
      option:
        - 2048
        - 4096